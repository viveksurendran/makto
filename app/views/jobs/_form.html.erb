<%= form_with(model: job, local: true) do |form| %>
  <% if job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
      <% job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :jobtitle %>
    <%= form.text_field :jobtitle, id: :job_jobtitle %>
  </div>

  <div class="field">
    <%= form.label :jobrole %>
    <%= form.text_field :jobrole, id: :job_jobrole %>
  </div>

  <div class="field">
    <%= form.label :jobresponsibility %>
    <%= form.text_area :jobresponsibility, id: :job_jobresponsibility %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
